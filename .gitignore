__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
build/
dist/
# ^ also covers macapp/build/ and macapp/dist/ (the built Collaborate.app),
# since git matches these anywhere in the tree, not just at the root
.macapp-build-venv/

# per-machine config — never shared via git
.collabconfig.yaml

.claude/
